To accomplish the task at hand in 9 I'm instructed to write a function "printReverse" that iterates over the char pointer in reverse. this requires declaring an arbitrary int x =0, and a while loop that iterates until there is a \0. We will then take the chars we discovered in the while loop and drive in reverse over them with a for (int i = x -1; i >= 0; i--) and then printf each iteration of the for loop. this will reseult in the string being displayed in reverse order.
