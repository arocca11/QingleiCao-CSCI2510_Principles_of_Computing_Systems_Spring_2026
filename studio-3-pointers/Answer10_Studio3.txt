char* reverseString(char* input) {
    int x = 0;
    while (input[x] != '\0') {
        x++;
    }

    char* output = (char*)malloc(x + 1);

    for (int i = 0; i < x; i++) {
        output[i] = input[x - 1 - i];
    }

    output[x] = '\0';

    return output;
}

